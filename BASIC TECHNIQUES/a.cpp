#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int a, b;
    string x;
    cin >> a >> b >> x;
    cout << "\n a is :" << a << "\n b is :" << b << "\n x is :" << x << "\n";

    // alternate solution 
    // int c, d;
    // scanf("%d %d", &c, &d);
    // printf("\n c is :%d d is :%d\n", c, d);
}